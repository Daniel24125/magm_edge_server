/**
 * ApplicationContext.tsx
 *
 * Responsibilities:
 * - Manages global application state (e.g., Page size | Page title).
 * 
 * Usage:
 * Wrap the root layout with <ApplicationProvider> and use useApplication()
 * in child components to access or update global state.
 */